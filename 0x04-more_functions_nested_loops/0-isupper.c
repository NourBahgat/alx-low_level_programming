#include "main.h"

/**
 * _isupper - determines upper case letters
 * @c: letter to be tested
 * Return: 0 if low, 1 if up
 */

int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
{
return (0);
}
}
