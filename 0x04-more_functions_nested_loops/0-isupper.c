#include "main.h"

/**
 * int _isupper - determines upper case letters
 * @c: letter to be tested
 * Return: 0 if low, 1 if up
 */

int _isupper(int c)
{
for (c = 'A'; c < 'Z'; c++)
{
return (1);
}
else
{
return (0);
}
}
