#include "main.h"
#include <ctype.h>
/**
 * _islower - determine lowercase characters
 * @c: contains value to be compared
 * Return: Always 0.
*/
int _islower(int c)
{
int d;
if (islower(d) == 0)
{
_putchar(48);
}
else
{
_putchar(49);
}
_putchar('\n');
return (0);
}
