#include "main.h"
#include <stdlib.h>

/**
 * _abs - prints the absolute of any number
 * abs - computes the absolute of int
 * @n: int to print
 * Return: n
*/

int _abs(int n)
{
n = abs(n);
return (n);
}
