#include "main.h"
#include <stdlib.h>

/**
 * _abs - prints the absolute of any number
 * abs - computes the absolute of a number 
 * @n: int to print
 * return: n
*/

int _abs(int n)
{
n = abs(n);
return (n);
}
